import {
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_CONTEXT_BOUND_CHILD_TRUTH_ADAPTER_V1,
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_CONTEXT_BOUND_CHILD_TRUTH_ADAPTER_VERSION_V1,
  deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereContextBoundChildTruthAdapterV1,
} from "./canonical-runtime-token-pos-current-snapshot-bound-where-context-bound-child-truth-adapter-v1.ts";
import {
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_AUTHORITY_BOUND_LEAF_TRUTH_EVIDENCE_V1,
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_AUTHORITY_BOUND_LEAF_TRUTH_EVIDENCE_VERSION_V1,
  deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereAuthorityBoundLeafTruthEvidenceV1,
} from "./canonical-runtime-token-pos-current-snapshot-bound-where-authority-bound-leaf-truth-evidence-v1.ts";

import {
  type CanonicalLanguageGraphV1,
  createCanonicalLanguageGraphV1 as createCanonicalLanguageGraphV1_V2,
  type GraphStatus as GraphStatus_V2,
  type LanguageGraphEdgeV1 as LanguageGraphEdgeV1_V2,
  type LanguageGraphNodeV1 as LanguageGraphNodeV1_V2,
} from "./canonical-language-graph-core-v1.ts";
import {
  buildCanonicalSurfaceDocumentV1 as buildCanonicalSurfaceDocumentV1_V2,
  type CanonicalSurfaceDocumentV1,
} from "./canonical-surface-boundary-v1.ts";
import type {
  CanonicalDependencyRuntimeManifestAuthorityRowV1,
} from "./canonical-dependency-runtime-authority-v1.ts";
import {
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_BINDING_OCCURRENCE_DOMAIN_AUTHORITY_V1,
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_BINDING_OCCURRENCE_DOMAIN_AUTHORITY_VERSION_V1,
  deriveCanonicalRuntimeTokenPosCurrentBindingOccurrenceDomainAuthorityV1,
} from "./canonical-runtime-token-pos-current-binding-occurrence-domain-authority-v1.ts";
import {
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_CONTEXT_SITE_OCCURRENCE_APPLICABILITY_EVIDENCE_V1,
  CANONICAL_RUNTIME_TOKEN_POS_CURRENT_CONTEXT_SITE_OCCURRENCE_APPLICABILITY_EVIDENCE_VERSION_V1,
  type CanonicalRuntimeTokenPosCurrentContextSiteOccurrenceApplicabilityEvidenceReadyResultV1,
  deriveCanonicalRuntimeTokenPosCurrentContextSiteOccurrenceApplicabilityEvidenceV1,
} from "./canonical-runtime-token-pos-current-context-site-occurrence-applicability-evidence-v1.ts";
import {
  deriveCanonicalGraphSnapshotIdentityAuthorityV1,
} from "./canonical-graph-snapshot-identity-authority-v1.ts";
import {
  type CanonicalRuntimeTokenPosNormalizedLabelEqAuthorityResultV1,
  deriveCanonicalRuntimeTokenPosNormalizedLabelEqAuthoritiesV1,
} from "./canonical-runtime-token-pos-normalized-label-eq-authority-v1.ts";
import {
  deriveCanonicalPosFactOwnershipAuthoritiesV1,
} from "./canonical-pos-fact-ownership-authority-v1.ts";
import {
  deriveCanonicalTokenPosPropertyCapabilityV1,
} from "./canonical-token-pos-property-capability-v1.ts";
import {
  deriveCanonicalDependencyRuntimeAuthoritiesV1,
} from "./canonical-dependency-runtime-authority-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingDefinitionAuthoritiesV1,
} from "./canonical-runtime-manifest-binding-definition-authority-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingWhereShapeAuthoritiesV1,
} from "./canonical-runtime-manifest-binding-where-shape-authority-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingWhereReferenceExpressionShapeAuthoritiesV1,
} from "./canonical-runtime-manifest-binding-where-reference-expression-shape-authority-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingWhereReferenceRootAuthoritiesV1,
} from "./canonical-runtime-manifest-binding-where-reference-root-authority-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingWhereLeafRightOperandSiteAuthoritiesV1,
} from "./canonical-runtime-manifest-binding-where-leaf-right-operand-site-authority-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingEntityCompatibilitiesV1,
} from "./canonical-runtime-manifest-binding-entity-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeManifestBindingScopeCompatibilitiesV1,
} from "./canonical-runtime-manifest-binding-scope-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeManifestTokenPosSuffixPropertyCompatibilitiesV1,
} from "./canonical-runtime-manifest-token-pos-suffix-property-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeManifestTokenPosStringOperandCompatibilitiesV1,
} from "./canonical-runtime-manifest-token-pos-string-operand-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeManifestTokenPosExactEqOperatorSourceCompatibilitiesV1,
} from "./canonical-runtime-manifest-token-pos-exact-eq-operator-source-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeManifestTokenPosExpectedNormalizedLabelEqAuthoritiesV1,
} from "./canonical-runtime-manifest-token-pos-expected-normalized-label-eq-authority-v1.ts";
import {
  deriveCanonicalSentenceDirectTokenOccurrenceDomainsV1,
} from "./canonical-sentence-direct-token-occurrence-domain-capability-v1.ts";
import {
  deriveCanonicalRuntimeManifestReferencedTokenSentenceDomainCompositionsV1,
} from "./canonical-runtime-manifest-referenced-token-sentence-domain-composition-v1.ts";
import {
  bindCanonicalRuntimeManifestReferencedTokenSentenceDomainsToSnapshotV1,
} from "./canonical-runtime-manifest-referenced-token-sentence-domain-snapshot-binding-v1.ts";
import {
  deriveCanonicalTokenPosGraphBoundNormalizedLabelProjectionV2,
} from "./canonical-token-pos-graph-bound-normalized-label-projection-v2.ts";
import {
  compareCanonicalTokenPosNormalizedLabelEvidenceV2,
} from "./canonical-token-pos-normalized-label-comparison-evidence-v2.ts";
import {
  deriveCanonicalRuntimeTokenPosSiteOccurrenceApplicabilityEvidenceV2,
} from "./canonical-runtime-token-pos-site-occurrence-applicability-evidence-v2.ts";
import {
  deriveCanonicalSnapshotSentenceOccurrenceIdentityAuthorityV1,
} from "./canonical-snapshot-sentence-occurrence-identity-authority-v1.ts";
import {
  deriveCanonicalCurrentRuntimeSentenceContextAuthorityV1,
} from "./canonical-current-runtime-sentence-context-authority-v1.ts";
import {
  deriveCanonicalRuntimeBindingDefinitionAuthoritiesV1,
} from "./canonical-runtime-binding-definition-authority-v1.ts";
import {
  deriveCanonicalRuntimeBindingWhereShapeAuthoritiesV1,
} from "./canonical-runtime-binding-where-shape-authority-v1.ts";
import {
  deriveCanonicalRuntimeBindingWhereReferenceRootAuthoritiesV1,
} from "./canonical-runtime-binding-where-reference-root-authority-v1.ts";
import {
  deriveCanonicalRuntimeBindingWhereRightOperandAuthoritiesV1,
} from "./canonical-runtime-binding-where-right-operand-authority-v1.ts";
import {
  deriveCanonicalRuntimeBindingWhereLeftRightSiteAuthoritiesV1,
} from "./canonical-runtime-binding-where-left-right-site-authority-v1.ts";
import {
  deriveCanonicalRuntimeBindingEntityCompatibilitiesV1,
} from "./canonical-runtime-binding-entity-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeTokenPosSuffixCompatibilitiesV1,
} from "./canonical-runtime-token-pos-suffix-compatibility-v1.ts";
import {
  deriveCanonicalRuntimeTokenPosStringOperandCompatibilitiesV1,
} from "./canonical-runtime-token-pos-string-operand-compatibility-v1.ts";
import type {
  CanonicalRuntimeManifestGraphNodeTypeAuthorityV1,
} from "./canonical-runtime-manifest-binding-entity-compatibility-v1.ts";
import type {
  CanonicalRuntimeManifestScopeBoundaryAuthorityV1,
} from "./canonical-runtime-manifest-binding-scope-compatibility-v1.ts";

function assert(
  condition: unknown,
  message: string,
): asserts condition {
  if (!condition) {
    throw new Error(message);
  }
}

Deno.test.ignore(
  "CURRENT WHERE authority-bound leaf truth V1 public identity and six-input API are exact",
  () => {
    assert(
      CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_AUTHORITY_BOUND_LEAF_TRUTH_EVIDENCE_V1 ===
        "canonical-runtime-token-pos-current-snapshot-bound-where-authority-bound-leaf-truth-evidence-v1",
      CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_AUTHORITY_BOUND_LEAF_TRUTH_EVIDENCE_V1,
    );

    assert(
      CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_AUTHORITY_BOUND_LEAF_TRUTH_EVIDENCE_VERSION_V1 ===
        "1.0.0",
      CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_AUTHORITY_BOUND_LEAF_TRUTH_EVIDENCE_VERSION_V1,
    );

    assert(
      deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereAuthorityBoundLeafTruthEvidenceV1
        .length ===
        6,
      String(
        deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereAuthorityBoundLeafTruthEvidenceV1
          .length,
      ),
    );
  },
);

Deno.test.ignore(
  "CURRENT WHERE authority-bound leaf truth V1 source contains no forbidden collection aggregation or referencePath shortcut",
  async () => {
    const source = await Deno.readTextFile(
      new URL(
        "./canonical-runtime-token-pos-current-snapshot-bound-where-authority-bound-leaf-truth-evidence-v1.ts",
        import.meta.url,
      ),
    );

    const forbidden = [
      /members\.every\s*\(/,
      /members\.some\s*\(/,
      /every\s*\([^)]*booleanTruth/,
      /some\s*\([^)]*booleanTruth/,
      /sourceReferencePath\s*===?\s*[^;\n]*manifestLeafPath/,
      /manifestLeafPath\s*===?\s*[^;\n]*sourceReferencePath/,
      /emptyBoundCollectionTruthInferred:\s*true/,
      /multipleBoundMemberTruthAggregated:\s*true/,
      /recursiveWhereCompositionPerformed:\s*true/,
      /compoundTruthComposed:\s*true/,
      /manifestConditionTruthResolved:\s*true/,
      /whereEvaluationPerformed:\s*true/,
    ];

    for (const pattern of forbidden) {
      assert(
        !pattern.test(source),
        `forbidden pattern found: ${pattern}`,
      );
    }
  },
);

Deno.test.ignore(
  "CURRENT WHERE authority-bound leaf truth V1 explicitly preserves fail-closed 0/1/N boundary",
  async () => {
    const source = await Deno.readTextFile(
      new URL(
        "./canonical-runtime-token-pos-current-snapshot-bound-where-authority-bound-leaf-truth-evidence-v1.ts",
        import.meta.url,
      ),
    );

    assert(
      source.includes(
        "collection.memberCount !== 1",
      ) &&
        source.includes(
          "empty_truth_not_specified",
        ) &&
        source.includes(
          "multiple_member_truth_aggregation_not_specified",
        ),
      "0/1/N fail-closed boundary missing",
    );
  },
);

Deno.test.ignore(
  "CURRENT WHERE authority-bound leaf truth V1 uses A4.6a1a authority id plus manifest leaf path as structural occurrence identity",
  async () => {
    const source = await Deno.readTextFile(
      new URL(
        "./canonical-runtime-token-pos-current-snapshot-bound-where-authority-bound-leaf-truth-evidence-v1.ts",
        import.meta.url,
      ),
    );

    assert(
      source.includes(
        "structuralAuthorityId:",
      ) &&
        source.includes(
          "structuralAuthority.id",
        ) &&
        source.includes(
          "nodePath:",
        ) &&
        source.includes(
          "structuralBridge.manifestLeafPath",
        ),
      "structural authority/path binding missing",
    );
  },
);
Deno.test.ignore(
  "WHERE authority-bound leaf truth evidence V1 cannot return ready with zero materialized evidence",
  async () => {
    const source = await Deno.readTextFile(
      new URL(
        "./canonical-runtime-token-pos-current-snapshot-bound-where-authority-bound-leaf-truth-evidence-v1.ts",
        import.meta.url,
      ),
    );

    const outputDeclarationIndex = source.indexOf(
      "const output:",
    );

    const zeroEvidenceGuardIndex = source.indexOf(
      "output.length === 0",
      outputDeclarationIndex,
    );

    const zeroEvidenceReasonIndex = source.indexOf(
      '"leaf_truth_evidence:no_materialized_evidence"',
      zeroEvidenceGuardIndex,
    );

    const readyEvidenceReturnIndex = source.indexOf(
      "evidence: output",
      zeroEvidenceReasonIndex,
    );

    assert(
      outputDeclarationIndex >= 0 &&
        zeroEvidenceGuardIndex >
          outputDeclarationIndex &&
        zeroEvidenceReasonIndex >
          zeroEvidenceGuardIndex &&
        readyEvidenceReturnIndex >
          zeroEvidenceReasonIndex,
      source,
    );

    assert(
      source.includes(
        "collection.memberCount === 0",
      ) &&
        source.includes(
          "bound_condition_collection:empty_truth_not_specified",
        ),
      "existing empty bound-collection fail-closed contract was lost",
    );

    assert(
      source.includes(
        "bound_condition_collection:multiple_member_truth_aggregation_not_specified",
      ),
      "existing N-member fail-closed contract was lost",
    );
  },
);

// -----------------------------------------------------------------------------
// Permanent real-producer singleton TRUE regression.
// Fixture source is intentionally limited to the canonical graph constructor,
// POS fixture, exact manifest row, and the actual producer chain.
// -----------------------------------------------------------------------------

function v2Assert(
  condition: unknown,
  message: string,
): asserts condition {
  if (!condition) {
    throw new Error(message);
  }
}

function v2BaseGraph() {
  const graph = createCanonicalLanguageGraphV1_V2(
    buildCanonicalSurfaceDocumentV1_V2(
      "x",
    ),
  );

  const token = graph.nodes.find(
    (node) =>
      node.type ===
        "token",
  );

  v2Assert(
    token !==
      undefined,
    "surface token missing",
  );

  return {
    graph,
    token,
  };
}

function v2PosFixture(
  options: {
    labels?: string[];
    statuses?: GraphStatus_V2[];
    alternativeStatus?:
      | "open"
      | "resolved"
      | "blocked";
    resolvedIndices?: number[];
  } = {},
) {
  const base = v2BaseGraph();

  const labels = options.labels ??
    [
      "noun",
      "adjective",
    ];

  const statuses = options.statuses ??
    labels.map(
      () => "candidate" as const,
    );

  const alternativeStatus = options.alternativeStatus ??
    "open";

  const resolvedIndices = options.resolvedIndices ??
    [];

  v2Assert(
    labels.length ===
      statuses.length,
    "v2PosFixture labels/status length mismatch",
  );

  const nodes: LanguageGraphNodeV1_V2[] = [];
  const edges: LanguageGraphEdgeV1_V2[] = [];
  const evidence: NonNullable<
    CanonicalLanguageGraphV1["evidence"]
  > = [];

  const memberIds: string[] = [];

  labels.forEach(
    (
      label,
      index,
    ) => {
      const lexicalId = `lex:${index}`;
      const posId = `pos:${index}`;

      const lexicalEvidenceId = `e:lex:${index}`;
      const posEvidenceId = `e:pos:${index}`;

      nodes.push({
        id: lexicalId,

        type: "lexical_reading",

        subtype: "lexical_candidate",

        status: "candidate",

        span: {
          ...base.token.span,
        },

        features: {
          sourcePos: label,
        },

        producer: "canonical_candidate_lattice_v1",

        evidenceIds: [
          lexicalEvidenceId,
        ],

        provenanceIds: [
          "prov:v2PosFixture",
        ],
      });

      nodes.push({
        id: posId,

        type: "lexical_reading",

        subtype: "pos_candidate",

        status: statuses[index],

        span: {
          ...base.token.span,
        },

        features: {
          pos: label,

          contributingLexicalReadingIds: [
            lexicalId,
          ],

          sourcePosIsEvidenceNotAuthority: true,
        },

        producer: "canonical_candidate_lattice_v1",

        evidenceIds: [
          posEvidenceId,
        ],

        provenanceIds: [
          "prov:v2PosFixture",
        ],
      });

      edges.push({
        id: `edge:lexical:${index}`,

        relation: "lexical_reading_of",

        sourceId: lexicalId,

        targetId: base.token.id,

        status: "candidate",

        features: {},

        producer: "canonical_candidate_lattice_v1",

        evidenceIds: [
          lexicalEvidenceId,
        ],

        provenanceIds: [
          "prov:v2PosFixture",
        ],
      });

      edges.push({
        id: `edge:pos:${index}`,

        relation: "pos_of",

        sourceId: posId,

        targetId: base.token.id,

        status: statuses[index],

        features: {
          pos: label,
        },

        producer: "canonical_candidate_lattice_v1",

        evidenceIds: [
          posEvidenceId,
        ],

        provenanceIds: [
          "prov:v2PosFixture",
        ],
      });

      edges.push({
        id: `support:${index}`,

        relation: "lexical_supports_pos",

        sourceId: lexicalId,

        targetId: posId,

        status: "candidate",

        features: {
          pos: label,
        },

        producer: "canonical_candidate_lattice_v1",

        evidenceIds: [
          posEvidenceId,
        ],

        provenanceIds: [
          "prov:v2PosFixture",
        ],
      });

      evidence.push(
        {
          id: lexicalEvidenceId,

          kind: "lexical",

          status: "supports",

          targetIds: [
            lexicalId,
            base.token.id,
          ],

          payload: {},

          producer: "canonical_candidate_lattice_v1",

          provenanceIds: [
            "prov:v2PosFixture",
          ],
        },
        {
          id: posEvidenceId,

          kind: "lexical",

          status: "supports",

          targetIds: [
            posId,
            lexicalId,
            base.token.id,
          ],

          payload: {
            pos: label,
          },

          producer: "canonical_candidate_lattice_v1",

          provenanceIds: [
            "prov:v2PosFixture",
          ],
        },
      );

      memberIds.push(
        posId,
      );
    },
  );

  const resolvedMemberIds = resolvedIndices.map(
    (index) => memberIds[index],
  );

  const graph: CanonicalLanguageGraphV1 = {
    ...base.graph,

    nodes: [
      ...base.graph.nodes,
      ...nodes,
    ],

    edges: [
      ...base.graph.edges,
      ...edges,
    ],

    evidence: [
      ...(base.graph.evidence ?? []),
      ...evidence,
    ],

    provenance: [
      ...(base.graph.provenance ?? []),
      {
        id: "prov:v2PosFixture",
        sourceType: "system",
        sourceId: "v2PosFixture",
      },
    ],

    alternativeSets: labels.length ===
        0
      ? []
      : [
        {
          id: `alt:pos:${base.token.id}`,

          memberIds,

          resolvedMemberIds,

          status: alternativeStatus,

          reason: "v2PosFixture",
        },
      ],
  };

  const ownership = deriveCanonicalPosFactOwnershipAuthoritiesV1(
    graph,
  );

  v2Assert(
    ownership.status ===
      "ready",
    `POS ownership v2PosFixture failed: ${
      JSON.stringify(
        ownership,
      )
    }`,
  );

  const capability = deriveCanonicalTokenPosPropertyCapabilityV1(
    ownership,
  );

  v2Assert(
    capability.status ===
      "ready",
    "POS capability v2PosFixture failed",
  );

  return {
    graph,
    tokenId: base.token.id,
    ownership,
    capability,
  };
}

function realSingletonTrueManifestRow(): CanonicalDependencyRuntimeManifestAuthorityRowV1 {
  return {
    id: "manifest-real-singleton-true",
    code: "test.real_singleton_true",
    authoring_status: "validated",
    runtime_family: "test",
    execution_phase: "test",
    constraint_strength: "required",

    actions: [
      {
        action: "create_dependency",
        target: "subject",
        relation: "test-relation",
        value: {
          source_ref: "subject",
          target_ref: "subject",
        },
      },
    ],

    ir_spec: {
      source: {
        primary_candidate_code: "test.real_singleton_true.source",
      },

      bindings: {
        subject: {
          entity: "token",
          scope: "sentence",
          cardinality: "one",

          where: {
            op: "eq",

            left: {
              ref: "subject.pos",
            },

            right: "noun",
          },
        },
      },
    },
  };
}

const manifestRows = [
  realSingletonTrueManifestRow(),
];

Deno.test(
  "CURRENT A4.6a1e real singleton TRUE preserves exact authority-bound evidence and CURRENT context",
  async () => {
    const fixture = v2PosFixture({
      labels: ["noun"],
      statuses: ["resolved"],
      alternativeStatus: "resolved",
      resolvedIndices: [0],
    });
    const surface = buildCanonicalSurfaceDocumentV1_V2("x");
    const graph = fixture.graph;
    const tokenId = fixture.tokenId;

    const dependencyResult = deriveCanonicalDependencyRuntimeAuthoritiesV1(
      manifestRows,
    );

    const manifestBindingResult =
      deriveCanonicalRuntimeManifestBindingDefinitionAuthoritiesV1(
        manifestRows,
      );

    const manifestWhereShapeResult =
      deriveCanonicalRuntimeManifestBindingWhereShapeAuthoritiesV1(
        manifestBindingResult,
      );

    const manifestReferenceExpressionResult =
      deriveCanonicalRuntimeManifestBindingWhereReferenceExpressionShapeAuthoritiesV1(
        manifestWhereShapeResult,
      );

    const manifestReferenceRootResult =
      deriveCanonicalRuntimeManifestBindingWhereReferenceRootAuthoritiesV1(
        manifestReferenceExpressionResult,
        manifestBindingResult,
      );

    const manifestLeafRightOperandResult =
      deriveCanonicalRuntimeManifestBindingWhereLeafRightOperandSiteAuthoritiesV1(
        manifestWhereShapeResult,
        manifestReferenceRootResult,
      );

    const nodeTypeAuthority: CanonicalRuntimeManifestGraphNodeTypeAuthorityV1 =
      {
        authorityId: "canonical-node-type:token",
        status: "proven",
        nodeType: "token",
        source: "canonical_language_graph_core_v1",
      };

    const manifestEntityCompatibilityResult =
      deriveCanonicalRuntimeManifestBindingEntityCompatibilitiesV1(
        manifestBindingResult,
        [nodeTypeAuthority],
      );

    const ownershipResult = deriveCanonicalPosFactOwnershipAuthoritiesV1(
      graph,
    );

    const posPropertyCapabilityResult =
      deriveCanonicalTokenPosPropertyCapabilityV1(
        ownershipResult,
      );

    const manifestTokenPosSuffixResult =
      deriveCanonicalRuntimeManifestTokenPosSuffixPropertyCompatibilitiesV1(
        manifestLeafRightOperandResult,
        manifestEntityCompatibilityResult,
        posPropertyCapabilityResult,
      );

    const manifestStringOperandResult =
      deriveCanonicalRuntimeManifestTokenPosStringOperandCompatibilitiesV1(
        manifestTokenPosSuffixResult,
      );

    const manifestExactEqResult =
      deriveCanonicalRuntimeManifestTokenPosExactEqOperatorSourceCompatibilitiesV1(
        manifestStringOperandResult,
      );

    const manifestExpectedResult =
      deriveCanonicalRuntimeManifestTokenPosExpectedNormalizedLabelEqAuthoritiesV1(
        manifestExactEqResult,
      );

    assert(
      manifestExpectedResult.status === "ready",
      "manifest expected not ready: " +
        JSON.stringify(manifestExpectedResult),
    );

    assert(
      manifestExpectedResult.candidates.length === 1,
      "manifest expected candidate count=" +
        manifestExpectedResult.candidates.length,
    );

    const actualResult =
      await deriveCanonicalTokenPosGraphBoundNormalizedLabelProjectionV2(
        surface,
        graph,
        tokenId,
      );

    const c2 = compareCanonicalTokenPosNormalizedLabelEvidenceV2(
      actualResult,
      manifestExpectedResult.candidates[0],
    );

    const directTokenDomainResult =
      deriveCanonicalSentenceDirectTokenOccurrenceDomainsV1(
        graph,
      );

    const sentenceBoundaryAuthority:
      CanonicalRuntimeManifestScopeBoundaryAuthorityV1 = {
        authorityId: "boundary:sentence",
        status: "proven",
        boundaryLabel: "sentence",
        source: "canonical_sentence_scope_boundary_capability_v1",
      };

    const manifestScopeResult =
      deriveCanonicalRuntimeManifestBindingScopeCompatibilitiesV1(
        manifestBindingResult,
        [sentenceBoundaryAuthority],
      );

    const manifestReferencedDomainResult =
      deriveCanonicalRuntimeManifestReferencedTokenSentenceDomainCompositionsV1(
        manifestTokenPosSuffixResult,
        manifestScopeResult,
        directTokenDomainResult,
      );

    const snapshotDomainResult =
      await bindCanonicalRuntimeManifestReferencedTokenSentenceDomainsToSnapshotV1(
        surface,
        graph,
        manifestReferencedDomainResult,
      );

    const d2a =
      deriveCanonicalRuntimeTokenPosSiteOccurrenceApplicabilityEvidenceV2(
        c2,
        snapshotDomainResult,
      );

    const snapshotIdentityResult =
      await deriveCanonicalGraphSnapshotIdentityAuthorityV1(
        surface,
        graph,
      );

    assert(
      snapshotIdentityResult.status === "ready" &&
        snapshotIdentityResult.authority !== undefined,
      "snapshot identity not ready",
    );

    const sentenceOccurrenceIdentityResult =
      await deriveCanonicalSnapshotSentenceOccurrenceIdentityAuthorityV1(
        surface,
        graph,
        snapshotIdentityResult,
      );

    assert(
      sentenceOccurrenceIdentityResult.status === "ready" &&
        sentenceOccurrenceIdentityResult.authorities.length === 1,
      "sentence occurrence identity not exactly one",
    );

    const executionInput = {
      executionInvocationId: "real-canonical-singleton-true-e2e",
      declaredCurrentSnapshotIdentityId:
        snapshotIdentityResult.authority.snapshotIdentityId,
      declaredCurrentSnapshotSentenceOccurrenceIdentityId:
        sentenceOccurrenceIdentityResult.authorities[0]
          .snapshotSentenceOccurrenceIdentityId,
    };

    const currentContextResult =
      await deriveCanonicalCurrentRuntimeSentenceContextAuthorityV1(
        surface,
        graph,
        executionInput,
      );

    const currentApplicabilityResult =
      deriveCanonicalRuntimeTokenPosCurrentContextSiteOccurrenceApplicabilityEvidenceV1(
        currentContextResult,
        d2a,
      );

    const currentDomainResult =
      deriveCanonicalRuntimeTokenPosCurrentBindingOccurrenceDomainAuthorityV1(
        currentApplicabilityResult,
      );

    const runtimeBindingResult =
      deriveCanonicalRuntimeBindingDefinitionAuthoritiesV1(
        dependencyResult.authorities,
        manifestRows,
      );

    const runtimeWhereShapeResult =
      deriveCanonicalRuntimeBindingWhereShapeAuthoritiesV1(
        runtimeBindingResult.authorities,
      );

    const runtimeReferenceRootResult =
      deriveCanonicalRuntimeBindingWhereReferenceRootAuthoritiesV1(
        runtimeWhereShapeResult.authorities,
        runtimeBindingResult.authorities,
      );

    const runtimeRightOperandResult =
      deriveCanonicalRuntimeBindingWhereRightOperandAuthoritiesV1(
        runtimeWhereShapeResult,
      );

    const runtimeLeftRightResult =
      deriveCanonicalRuntimeBindingWhereLeftRightSiteAuthoritiesV1(
        runtimeReferenceRootResult,
        runtimeRightOperandResult,
      );

    const runtimeEntityCompatibilityResult =
      deriveCanonicalRuntimeBindingEntityCompatibilitiesV1(
        runtimeBindingResult.authorities,
        [
          {
            authorityId: "canonical-node-type:token",
            status: "proven",
            nodeType: "token",
            source: "canonical_language_graph_core_v1",
          },
        ],
      );

    const runtimeTokenPosSuffixResult =
      deriveCanonicalRuntimeTokenPosSuffixCompatibilitiesV1(
        runtimeReferenceRootResult,
        runtimeEntityCompatibilityResult,
        posPropertyCapabilityResult,
      );

    const runtimeStringOperandResult =
      deriveCanonicalRuntimeTokenPosStringOperandCompatibilitiesV1(
        runtimeLeftRightResult,
        runtimeTokenPosSuffixResult,
      );

    const expectedResult =
      deriveCanonicalRuntimeTokenPosNormalizedLabelEqAuthoritiesV1(
        runtimeStringOperandResult,
      );

    assert(
      expectedResult.status === "ready",
      "runtime expected not ready: " +
        JSON.stringify(expectedResult),
    );

    assert(
      expectedResult.authorities.length === 1,
      "runtime expected count=" +
        expectedResult.authorities.length,
    );

    v2Assert(
      manifestExpectedResult.status ===
          "ready" &&
        manifestExpectedResult.candidates.length ===
          1,
      "real manifest expected authority was not uniquely produced",
    );

    v2Assert(
      currentDomainResult.status ===
          "ready" &&
        currentDomainResult.authority.bindingOccurrenceDomains.length ===
          1,
      "real CURRENT binding occurrence domain was not uniquely produced",
    );

    v2Assert(
      expectedResult.status ===
          "ready" &&
        expectedResult.authorities.length ===
          1,
      "real runtime expected authority was not uniquely produced",
    );

    const realDomain =
      currentDomainResult.authority.bindingOccurrenceDomains[0];

    const realExpected = expectedResult.authorities[0];

    v2Assert(
      realDomain.manifestId ===
          realExpected.manifestId &&
        realDomain.manifestCode ===
          realExpected.manifestCode &&
        realDomain.referencedBindingName ===
          realExpected.referencedBindingName,
      "shared structural/source identity must match across producer families",
    );

    v2Assert(
      realDomain.referencedBindingDefinitionAuthorityId !==
        realExpected.referencedBindingDefinitionAuthorityId,
      "producer-family binding-definition authority IDs must remain distinct",
    );

    v2Assert(
      realDomain.referencedBindingDefinitionAuthorityId.startsWith(
        "runtime-manifest-binding-definition-v1:",
      ),
      "CURRENT domain did not preserve manifest-family binding-definition authority",
    );

    v2Assert(
      realExpected.referencedBindingDefinitionAuthorityId.startsWith(
        "runtime-binding-definition-authority-v1:",
      ),
      "runtime expected authority did not preserve runtime-family binding-definition authority",
    );

    const candidate =
      await deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereAuthorityBoundLeafTruthEvidenceV1(
        surface,
        graph,
        expectedResult,
        currentDomainResult,
        dependencyResult.authorities,
        manifestRows,
      );

    v2Assert(
      candidate.status === "ready",
      `real producer Candidate blocked: ${JSON.stringify(candidate)}`,
    );

    v2Assert(
      candidate.authority.evidence.length === 1,
      `real producer Candidate evidence count=${candidate.authority.evidence.length}`,
    );

    v2Assert(
      candidate.authority.evidence[0]
        .childTruthEvidence
        .truthDisposition ===
        "resolved_true",
      `real producer Candidate did not materialize resolved_true: ${
        JSON.stringify(candidate.authority.evidence[0])
      }`,
    );

    const serializedCandidate = JSON.stringify(candidate);

    v2Assert(
      serializedCandidate.includes(
        '"recursiveWhereCompositionPerformed":false',
      ),
      "Candidate unexpectedly performed recursive WHERE composition",
    );

    v2Assert(
      serializedCandidate.includes(
        '"compoundTruthComposed":false',
      ),
      "Candidate unexpectedly composed compound truth",
    );

    v2Assert(
      serializedCandidate.includes(
        '"manifestConditionTruthResolved":false',
      ),
      "Candidate unexpectedly resolved manifest condition truth",
    );

    v2Assert(
      serializedCandidate.includes(
        '"runtimeConditionTruthResolved":false',
      ),
      "Candidate unexpectedly resolved runtime condition truth",
    );

    v2Assert(
      serializedCandidate.includes(
        '"bindingTruthResolved":false',
      ),
      "Candidate unexpectedly resolved binding truth",
    );

    v2Assert(
      serializedCandidate.includes(
        '"whereEvaluationPerformed":false',
      ),
      "Candidate unexpectedly performed WHERE evaluation",
    );

    v2Assert(
      !serializedCandidate.includes(
        '"learnerErrorClassified":true',
      ),
      "Candidate unexpectedly classified learner error",
    );

    v2Assert(
      !serializedCandidate.includes(
        '"graphMutationPerformed":true',
      ),
      "Candidate unexpectedly mutated graph",
    );

    const contextBoundCandidate =
      await deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereContextBoundChildTruthAdapterV1(
        surface,
        graph,
        expectedResult,
        currentDomainResult,
        dependencyResult.authorities,
        manifestRows,
      );

    v2Assert(
      contextBoundCandidate.status === "ready",
      `CURRENT A4.6a1e adapter blocked: ${
        JSON.stringify(contextBoundCandidate)
      }`,
    );

    if (contextBoundCandidate.status !== "ready") {
      throw new Error("CURRENT A4.6a1e adapter unexpectedly blocked");
    }

    v2Assert(
      contextBoundCandidate.evidenceCount === 1 &&
        contextBoundCandidate.evidence.length === 1,
      `CURRENT A4.6a1e evidence count=${contextBoundCandidate.evidence.length}`,
    );

    const contextBoundEvidence = contextBoundCandidate.evidence[0];

    v2Assert(
      contextBoundEvidence !== undefined,
      "CURRENT A4.6a1e evidence missing",
    );

    const sourceLeaf = contextBoundCandidate.sourceResult.authority.evidence[0];

    v2Assert(
      sourceLeaf !== undefined,
      "source CURRENT leaf evidence missing",
    );

    v2Assert(
      sourceLeaf.authorityBoundChildTruth.status === "ready",
      "source CURRENT authority-bound child truth not ready",
    );

    if (
      sourceLeaf.authorityBoundChildTruth.status !== "ready"
    ) {
      throw new Error(
        "source CURRENT authority-bound child truth unexpectedly blocked",
      );
    }

    const sourceAuthorityBoundEvidence =
      sourceLeaf.authorityBoundChildTruth.evidence;

    v2Assert(
      contextBoundEvidence.authorityBoundChildTruthEvidence ===
        sourceAuthorityBoundEvidence,
      "A4.6a1e did not preserve exact authority-bound evidence object reference",
    );

    v2Assert(
      contextBoundEvidence.structuralAuthorityId ===
          sourceAuthorityBoundEvidence.structuralAuthorityId &&
        contextBoundEvidence.nodePath ===
          sourceAuthorityBoundEvidence.nodePath,
      "A4.6a1e structural authority/path mismatch",
    );

    v2Assert(
      contextBoundEvidence.snapshotIdentityId ===
          currentDomainResult.authority.snapshotIdentityId &&
        contextBoundEvidence.snapshotSentenceOccurrenceIdentityId ===
          currentDomainResult.authority
            .snapshotSentenceOccurrenceIdentityId,
      "A4.6a1e CURRENT context composite identity mismatch",
    );

    v2Assert(
      contextBoundEvidence.graphDocumentId ===
          currentDomainResult.authority.graphDocumentId &&
        contextBoundEvidence.sentenceNodeId ===
          currentDomainResult.authority.sentenceNodeId,
      "A4.6a1e CURRENT context consistency metadata mismatch",
    );

    v2Assert(
      contextBoundEvidence.provenance
        .authorityBoundChildTruthEvidenceObjectPreservedWithoutReconstruction ===
        true,
      "A4.6a1e exact wrapped evidence preservation provenance missing",
    );

    v2Assert(
      contextBoundEvidence.provenance
            .authorityContextPairProvenBySourceAdapter ===
          true &&
        contextBoundEvidence.provenance
            .contextInferredByInterface ===
          false &&
        contextBoundEvidence.provenance
            .callerSuppliedContextAcceptedAsProof ===
          false,
      "A4.6a1e authority/context proof boundary mismatch",
    );

    v2Assert(
      contextBoundEvidence.provenance
            .truthDispositionDuplicatedAtContextBindingLayer ===
          false &&
        contextBoundEvidence.provenance
            .truthRecomputedByInterface ===
          false,
      "A4.6a1e unexpectedly duplicated or recomputed truth",
    );

    v2Assert(
      !("sentenceIndex" in contextBoundEvidence),
      "sentenceIndex leaked into A4.6a1e context evidence",
    );

    v2Assert(
      !("snapshotTokenOccurrenceIdentityId" in contextBoundEvidence),
      "token occurrence identity leaked into sentence context evidence",
    );

    v2Assert(
      sourceLeaf.childTruthEvidence.truthDisposition ===
        "resolved_true",
      "source real singleton truth was not resolved_true",
    );

    const serializedContextBound = JSON.stringify(contextBoundCandidate);

    v2Assert(
      serializedContextBound.includes(
        '"recursiveWhereCompositionPerformed":false',
      ) &&
        serializedContextBound.includes(
          '"compoundTruthComposed":false',
        ) &&
        serializedContextBound.includes(
          '"runtimeConditionTruthResolved":false',
        ) &&
        serializedContextBound.includes(
          '"bindingTruthResolved":false',
        ) &&
        serializedContextBound.includes(
          '"whereEvaluationPerformed":false',
        ),
      "CURRENT A4.6a1e semantic ceiling mismatch",
    );
  },
);

Deno.test(
  "CURRENT A4.6a1e adapter public identity and six-input API are exact",
  () => {
    assert(
      CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_CONTEXT_BOUND_CHILD_TRUTH_ADAPTER_V1 ===
        "canonical-runtime-token-pos-current-snapshot-bound-where-context-bound-child-truth-adapter-v1",
      "producer mismatch",
    );

    assert(
      CANONICAL_RUNTIME_TOKEN_POS_CURRENT_SNAPSHOT_BOUND_WHERE_CONTEXT_BOUND_CHILD_TRUTH_ADAPTER_VERSION_V1 ===
        "1.0.0",
      "version mismatch",
    );

    assert(
      deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereContextBoundChildTruthAdapterV1
        .length === 6,
      "adapter must expose exact six-input CURRENT API",
    );
  },
);

Deno.test(
  "CURRENT A4.6a1e adapter source preserves authority/context boundary",
  async () => {
    const source = await Deno.readTextFile(
      new URL(
        "./canonical-runtime-token-pos-current-snapshot-bound-where-context-bound-child-truth-adapter-v1.ts",
        import.meta.url,
      ),
    );

    assert(
      source.includes(
        "deriveCanonicalRuntimeTokenPosCurrentSnapshotBoundWhereAuthorityBoundLeafTruthEvidenceV1(",
      ),
      "closed CURRENT leaf producer must be reexecuted",
    );

    assert(
      source.includes(
        "sourceCurrentContextApplicabilityResult",
      ) &&
        source.includes(
          "sourceCurrentContextResult",
        ),
      "exact preserved CURRENT context lineage must be consumed",
    );

    assert(
      source.includes(
        "authorityBoundChildTruthEvidenceObjectPreservedWithoutReconstruction",
      ),
      "exact authority-bound evidence preservation must be explicit",
    );

    assert(
      !source.includes(
        "sentenceIndexUsedAsContextIdentity: true",
      ) &&
        !source.includes(
          "snapshotTokenOccurrenceIdentityUsedAsSentenceContextIdentity: true",
        ),
      "narrower occurrence metadata must not become context identity",
    );

    const forbidden = [
      /\bas\s+unknown\s+as\b/,
      /\bas\s+any\b/,
      /:\s*any\b/,
      /<any>/,
      /\.every\s*\(/,
      /\.some\s*\(/,
    ];

    for (const pattern of forbidden) {
      assert(
        !pattern.test(source),
        `forbidden source pattern: ${pattern}`,
      );
    }
  },
);

Deno.test(
  "CURRENT A4.6a1e adapter semantic ceiling remains below recursive WHERE composition",
  async () => {
    const source = await Deno.readTextFile(
      new URL(
        "./canonical-runtime-token-pos-current-snapshot-bound-where-context-bound-child-truth-adapter-v1.ts",
        import.meta.url,
      ),
    );

    assert(
      source.includes(
        "recursiveWhereCompositionPerformed: false",
      ) &&
        source.includes(
          "compoundTruthComposed: false",
        ) &&
        source.includes(
          "manifestConditionTruthResolved: false",
        ) &&
        source.includes(
          "runtimeConditionTruthResolved: false",
        ) &&
        source.includes(
          "bindingTruthResolved: false",
        ) &&
        source.includes(
          "whereEvaluationPerformed: false",
        ) &&
        source.includes(
          "learnerErrorClassified: false",
        ) &&
        source.includes(
          "graphMutationPerformed: false",
        ),
      "semantic ceiling mismatch",
    );
  },
);
